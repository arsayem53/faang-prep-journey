# lc1365_smaller_numbers.py
def smaller_number(arr):
    count = [] * len(arr)
    for i in range(len(arr)):
    nums = 0
        for j in range(i+1, len(arr)):
            if nums[i] > nums[j]:
               nums += 1
        count[i] = nums
    return count